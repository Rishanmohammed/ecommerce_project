<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>E-Shop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/style.css">
</head>

<body class="d-flex flex-column min-vh-100">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">🛒 E-Shop</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-lg-center">
          {{#if user}}
            <li class="nav-item me-3">
              <span class="text-white">Welcome, <strong>{{user.name}}</strong></span>
            </li>
            <li class="nav-item me-2">
              <a class="btn btn-light btn-sm" href="/orders">My Orders</a>
            </li>
            <li class="nav-item me-2">
              <a class="btn btn-outline-light btn-sm position-relative" href="/cart">
                Cart
                {{#if cart}}
                  {{#if cart.length}}
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-warning text-dark">
                      {{cart.length}}
                    </span>
                  {{/if}}
                {{/if}}
              </a>
            </li>
            <li class="nav-item">
              <a class="btn btn-outline-light btn-sm" href="/auth/logout">Logout</a>
            </li>
          {{else}}
            <li class="nav-item me-2">
              <a class="btn btn-light btn-sm" href="/auth/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-outline-light btn-sm" href="/auth/register">Register</a>
            </li>
          {{/if}}
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="container my-5 flex-grow-1">
    {{{body}}}
  </main>

  <!-- Footer -->
  <footer class="bg-primary text-white text-center py-4 mt-auto">
    <div class="container">
      <p class="mb-1">© {{year}} <strong>Developed By Rishan</strong></p>
      <small class="text-light">Built with ❤️ using Node.js, Express & MongoDB</small>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
